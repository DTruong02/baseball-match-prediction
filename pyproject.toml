[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "baseball-analyze"
version = "0.1.0"
description = "Pregame MLB home win probability from MLB Stats API + FanGraphs (pybaseball)"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.27",
    "numpy>=1.24",
    "pandas>=2.0",
    "pybaseball>=2.2",
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "scikit-learn>=1.3",
    "typer>=0.12",
    "joblib>=1.3",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]
chat = ["openai>=1.0"]
backend = [
    "alembic>=1.13",
    "bcrypt>=4.0",
    "email-validator>=2.0",
    "fastapi>=0.115",
    "psycopg2-binary>=2.9",
    "pydantic-settings>=2.0",
    "python-jose[cryptography]>=3.3",
    "python-multipart>=0.0.9",
    "sqlalchemy>=2.0",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
]

[tool.pytest.ini_options]
pythonpath = ["backend"]

[project.scripts]
baseball-analyze = "baseball_analyze.cli:main"
baseball-analyze-train = "baseball_analyze.train:main_train"
baseball-api = "baseball_backend.main:main"
baseball-sync-schedule = "baseball_backend.sync_schedule:main"

[tool.setuptools.packages.find]
where = ["src", "backend"]
